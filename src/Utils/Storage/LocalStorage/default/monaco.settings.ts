const defaultSettings: defaultMonacoSettings = {
    cursorStyle: "line-thin",
    cursorBlinking: "smooth",
    fontSize: 14,
    renderWhitespace: "all",
    smoothScrolling: true,
}

export default defaultSettings;